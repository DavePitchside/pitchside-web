export const metadata = {
  title: "Account Deletion",
  description: "Request deletion of a Pitchside AI account and its associated data.",
  alternates: { canonical: "/account-deletion" },
  robots: { index: false, follow: false },
};

export default function AccountDeletionLayout({ children }) {
  return children;
}
